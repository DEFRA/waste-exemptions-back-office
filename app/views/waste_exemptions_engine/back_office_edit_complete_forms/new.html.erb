<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">
    <%= form_for(@back_office_edit_complete_form,
                 url: main_app.back_office_edit_complete_forms_path(token: @back_office_edit_complete_form.token)) do |f| %>
      <%= render partial: "waste_exemptions_engine/shared/error_summary", locals: { f: f } %>

      <h1 class="govuk-heading-l"><%= t(".heading") %></h1>

      <p class="govuk-body"><%= t(".paragraph_1") %></p>

      <%= link_to t(".next_button"), edit_finished_path(@back_office_edit_complete_form.transient_registration), class: "govuk-button" %>
    <% end %>
  </div>
</div>
