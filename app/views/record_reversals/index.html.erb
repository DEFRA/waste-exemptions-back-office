<%= render "shared/payments_table",
           payments: @payments,
           resource: @resource,
           show_available_amount: false,
           translation_prefix: "record_reversals.index" do |payment| %>
  <%= link_to new_registration_record_reversal_path(reference: @resource.reference, payment_id: payment.id) do %>
    <%= t("record_reversals.index.reverse_payment") %>
  <% end %>
<% end %>
